# Admin Panel Credentials
# IMPORTANT: Copy this file to .env and change these values!

# Admin username
ADMIN_USERNAME=admin

# Admin password (plaintext - will be hashed automatically)
# Change this to a strong password before deploying!
ADMIN_PASSWORD=changeme123

# Generate a random string for session encryption
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-random-secret-key-at-least-32-characters-long

# Node environment (development or production)
NODE_ENV=development

# Server port (optional, defaults to 3000)
# PORT=3000
